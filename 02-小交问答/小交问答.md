“我是零基础小白也能参赛吗？” 在听闻了众多自称“小白”的同学的疑问后，小交同学决定在校内赛插入一道简答的选择填空题来让真正的零基础小白有参与感。但是如果只是选择填空题怎么体现 HPC 的主题的呢？小交陷入了沉思，或许可以让大家使用一些独特的方式提交答案。

本题共有 9 道小题，小交为了避免大家作弊，要求在提交的 writeup 中简要说明你 3-9 小题的解题过程（如果你没有进行任何实操就得到了答案则不需要写）。

1、[5'] 以下哪个不是大模型推理引擎？

A. vLLM B. LibreChat C. SGLang D. llama.cpp

2、[5'*2] 某高性能计算集群需要通过带宽为 10 Gbps 的网络传输一个大小为 4 GiB 的文件。该文件大小相当于（ ）GB，忽略协议开销，理论传输时间为（ ）秒。（均保留一位小数）

3、[5'*2] NVIDIA GeForce RTX 5090 的 Tensor Core 在执行 FP8 矩阵乘法、采用 FP16 累加且不使用结构化稀疏的情况下，理论峰值吞吐量是（ ）TFLOPS？如果保持其他条件不变，采用 FP32 累加，理论峰值吞吐量是（ ）TFLOPS？

4、[5'*2] kp_run 队列节点实际使用的以太网卡的名称是（ ），其固件版本为（ ）

5、[5'*2] kp_run 队列节点的默认 C/C++ 编译器版本是（ ）（点分隔字符串），其主程序的入口地址为（ ）（0x开头十六进制数）

6、[5';10'] 中国超算“灵晟”集群所采用的 LX2 CPU 每 NUMA 配置 （ ）核心，单 NUMA FP64 理论算力为（ ）GFLOPS（保留一位小数）

7、[10'] 在 2026 年 9 月 1 日前，网址 https://xflops.sjtu.edu.cn/xflops-htop.png 的内容的 SHA256 校验值（一串小写的16进制数字）是（ ）？

8、[5'*2] 参考 CUDA 12.6 的 NVIDIA Hopper Tuning Guide，考虑 NVIDIA H100 上两个 CUDA kernel。两者每个线程块均为 256 个线程，每个线程分别需要 64 个和 65 个 32 位寄存器。仅考虑每个 SM 的寄存器总量及最大驻留 warp 数限制，两个 kernel 的理论最大 occupancy 分别为（ ）% 和（ ）%。（均保留 1 位小数）（Tip：occupancy 定义为驻留 warp 数与硬件最大驻留 warp 数之比。线程块必须完整驻留；本题忽略寄存器分配粒度取整及其他资源限制。）

9、[10'*2] 根据 DeepSeek-V4-Flash-0731 的模型结构，单条请求在 decode 时的上下文长度为 524288 token，包含本次输入的 token。

在单张 NVIDIA B200 SXM 上，第 4 个 Transformer 层（从 1 编号）的 sparse attention 理论最小计算时间为（ ）μs（保留3位有效数字）；保存该请求所有层的主注意力 KV cache 共需（ ）GiB 显存（保留3位有效数字）。

Tip：

结构及张量布局以给出链接的提交的 inference/config.json、model.py 和 kernel.py 为准。默认浮点类型为 BF16，world_size=max_batch_size=1，max_seq_len=524288；第二空仅统计 model.layers 中各层 attn.kv_cache 张量的数据存储大小。
第一空以 sparse_attn 的数学语义为准，采用理想 Roofline 模型：仅计矩阵乘法运算量（FMA 计 2 FLOPs）及必要输入张量的读取、输出张量的写回；每份数据仅访问 HBM 一次，中间结果保留在片上，计算与访存完全重叠，其余开销忽略。输入均已生成且初始仅驻留 HBM，不受参考代码的具体线程块调度限制。
硬件取 NVIDIA 官方规格中的峰值算力及 HBM 带宽，硬件指标采用 SI 单位。